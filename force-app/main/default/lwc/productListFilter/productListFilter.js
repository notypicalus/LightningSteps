import { LightningElement } from 'lwc';

// A Component to filter the products based on chosen attributes 
// (e.g. category: boot, running shoe, etc.; price).

export default class ProductListFilter extends LightningElement {}